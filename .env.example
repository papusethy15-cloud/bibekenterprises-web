# Copy this file to .env.local and fill in your values.
# Each domain deployment has its own .env.local with its own DOMAIN_SLUG.

NEXT_PUBLIC_DOMAIN_SLUG=your-domain-slug
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
